sourceSets {
    main {
        groovy {
            srcDir 'src/main/groovy'
        }
    }
}

task myJar(type: Jar) {
    baseName = 'jar-from-b'
    //classifier = 'dep'
    from project.sourceSets.main.output
}

artifacts {
    myArchives myJar
}